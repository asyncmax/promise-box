{
  "name": "promise-box",
  "version": "0.1.3",
  "description": "A collection of essential Promise utilities",
  "author": "Max Jung <max@gourmetjs.com>",
  "license": "MIT",
  "repository": "https://github.com/asyncmax/promise-box",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint lib/*.js test/**/*.js examples/*.js",
    "tape": "tape test/*.js",
    "test": "npm run lint && npm run tape"
  },
  "devDependencies": {
    "eslint": "^3.5.0",
    "tape": "^4.6.0"
  }
}
